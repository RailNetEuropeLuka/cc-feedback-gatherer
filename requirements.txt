# Root requirements for Streamlit Community Cloud (it looks here at the repo root).
# Mirrors feedback_gatherer/requirements.txt, plus pandas for the app's tables.
streamlit>=1.58
extract-msg>=0.55
python-docx>=1.2
PyMuPDF>=1.27
openpyxl>=3.1
rapidfuzz>=3.14
PyYAML>=6.0
pandas>=2.0
